POSTGRES_USER=eapproval
# Sinh mật khẩu mạnh: openssl rand -base64 24 | tr -d '/+=' | head -c 32 — KHÔNG dùng giá trị mẫu ở môi trường thật
POSTGRES_PASSWORD=eapproval
POSTGRES_DB=eapproval
POSTGRES_PORT=5432
