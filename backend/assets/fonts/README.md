DejaVu Sans (Regular + Bold), sao chép từ gói hệ thống `fonts-dejavu-core` (`/usr/share/fonts/truetype/dejavu/`).
Dùng để nhúng vào PDF khi đóng dấu/chữ ký (`lib/stamp.ts`) — hỗ trợ đầy đủ dấu tiếng Việt, giấy phép tự do (Bitstream Vera License), được phép nhúng/phân phối lại.
